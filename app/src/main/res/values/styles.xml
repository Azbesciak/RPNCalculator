<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorNumbers">@color/colorNumbersLight</item>
        <item name="colorActions">@color/colorActionsLight</item>
        <item name="colorOperators">@color/colorOperatorsLight</item>
        <item name="buttonStyle">@style/ButtonFontStyle</item>
        <item name="colorStack">@android:color/primary_text_light</item>
    </style>
    <style name="ButtonFontStyle" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textSize">18sp</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DarkTheme" parent="Theme.AppCompat" >
        <item name="colorNumbers">@color/colorNumbersDark</item>
        <item name="colorActions">@color/colorActionsDark</item>
        <item name="colorOperators">@color/colorOperatorsDark</item>
        <item name="buttonStyle">@style/ButtonFontStyle</item>
        <!--<item name="android:textViewStyle">@style/DarkTextView</item>-->
        <item name="colorStack">@android:color/primary_text_dark</item>
    </style>

    <style name="DarkTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DarkTextView" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@android:color/primary_text_dark</item>
    </style>

</resources>
